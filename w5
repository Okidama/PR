#include <iostream>
using namespace std;

int main() {
	setlocale(0, "");
	int num = 1;
	cout << "Введите число: "; cin >> num;
	for (int i = 0; i < 10; i++) {
		cout << num << " * " << i + 1 << " = " << num * (i + 1) << endl;
	}
}
