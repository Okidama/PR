#include <iostream>
using namespace std;

int main() {
	setlocale(0, "");
	int num;
	cout << "Введите число: "; cin >> num;
	for (int i = num; i > 0; i--) {
		cout << "Отсчет: " << i << endl;
	}
	cout << "Старт!";
}
